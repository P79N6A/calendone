import React from "react";

import "./index.less";

interface IProps {}

const Nav: React.FunctionComponent = (props: IProps) => {
    return <div className="nav-side">nav</div>;
};

export default Nav;
